docker-machine ssh Char
docker swarm init --advertise-addr $(ip)
docker swarm join-token worker



# use the command $(docker-machine ip) Char to get ip of the machine
# user the $(docker swarm join-token worker) to get the join token to make worker nodes 